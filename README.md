pending tasks:
1. fix email verification --> problem: resend is paid 
2. ai integration --> problem : anthropic and openai api have limit 
3. create a new page /public/[usename]/page.tsx--> hit backend-->/api/send-message
4. display ai generated messages on this page on clicking "suggest message " button 
5. on clicking on the message it will copied to the " text area "